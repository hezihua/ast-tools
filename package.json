{
  "name": "ast-tools",
  "version": "1.0.0",
  "description": "AST tools monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter ast-explorer-web dev",
    "build": "pnpm -r build",
    "build:core": "pnpm --filter @ast-tools/core build",
    "build:web": "pnpm --filter ast-explorer-web build",
    "start": "pnpm --filter ast-explorer-web start",
    "lint": "pnpm -r lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
